import React,{useEffect,useState} from 'react';import{useParams}from'react-router-dom';import{toast}from'react-toastify';import api from'../../services/api';import StatusBadge from'../../components/StatusBadge';const statuses=['Applied','Under Review','Shortlisted','Interview Scheduled','Rejected','Hired'];export default function Applicants(){const{jobId}=useParams();const[i,setI]=useState([]);const load=()=>api.get(`/recruiter/applicants/${jobId}`).then(r=>setI(r.data));useEffect(()=>{load()},[jobId]);const upd=async(id,status)=>{await api.put(`/recruiter/application/${id}/status`,{status});toast.success('Updated');load()};return <div><h1 className="text-3xl font-black">Applicants</h1><div className="mt-6 space-y-3">{i.map(a=><div className="card p-5" key={a._id}><div className="flex justify-between"><div><b>{a.candidate?.name}</b><p>Match: {a.matchScore||0}%</p></div><StatusBadge status={a.status}/></div><div className="mt-4 flex flex-wrap gap-2">{statuses.map(s=><button className="btn-secondary py-2 text-xs" key={s} onClick={()=>upd(a._id,s)}>{s}</button>)}</div></div>)}</div></div>}
