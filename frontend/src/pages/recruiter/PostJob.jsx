import React,{useState} from 'react';import{toast}from'react-toastify';import api from'../../services/api';const init={title:'',description:'',location:'',jobType:'Internship',experienceLevel:'Entry',salaryMin:'',salaryMax:'',deadline:'',skills:[]};export default function PostJob(){const[f,setF]=useState(init),[skill,setSkill]=useState('');const add=()=>{if(skill.trim()){setF({...f,skills:[...new Set([...f.skills,skill.trim()])]});setSkill('')}};const submit=async e=>{e.preventDefault();try{await api.post('/jobs',f);toast.success('Job posted');setF(init)}catch(err){toast.error(err.response?.data?.message||'Failed')}};return <div><h1 className="text-3xl font-black">Post Job</h1><form onSubmit={submit} className="card mt-6 grid gap-4 p-6"><input className="input" placeholder="Title" value={f.title} onChange={e=>setF({...f,title:e.target.value})}/><textarea className="input min-h-32" placeholder="Description (min 50 chars)" value={f.description} onChange={e=>setF({...f,description:e.target.value})}/><input className="input" placeholder="Location" value={f.location} onChange={e=>setF({...f,location:e.target.value})}/><input className="input" type="date" value={f.deadline} onChange={e=>setF({...f,deadline:e.target.value})}/><div className="flex gap-2"><input className="input" placeholder="Skill" value={skill} onChange={e=>setSkill(e.target.value)}/><button type="button" onClick={add} className="btn-secondary">Add</button></div><button className="btn-primary">Post</button></form></div>}
