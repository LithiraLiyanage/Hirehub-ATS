import{useEffect,useState}from'react';import api from'../../services/api';import StatusBadge from'../../components/StatusBadge';export default function AdminApplications(){const[i,setI]=useState([]);useEffect(()=>{api.get('/admin/applications').then(r=>setI(r.data))},[]);return <div><h1 className="text-3xl font-black">Applications</h1><div className="mt-6 space-y-3">{i.map(a=><div className="card flex justify-between p-5" key={a._id}><span>{a.candidate?.name} → {a.job?.title}</span><StatusBadge status={a.status}/></div>)}</div></div>}
