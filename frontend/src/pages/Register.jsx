import React,{useState} from 'react';import{useNavigate,Link}from'react-router-dom';import{toast}from'react-toastify';import{useAuth}from'../context/AuthContext';export default function Register(){const{register}=useAuth();const nav=useNavigate();const[f,setF]=useState({name:'',email:'',password:'',confirmPassword:'',role:'candidate'});const submit=async e=>{e.preventDefault();if(f.password!==f.confirmPassword)return toast.error('Passwords do not match');try{const p=await register({name:f.name,email:f.email,password:f.password,role:f.role});toast.success('Account created');nav(p)}catch(err){toast.error(err.response?.data?.message||'Registration failed')}};return <main className="flex min-h-[80vh] items-center justify-center px-4"><form onSubmit={submit} className="card w-full max-w-lg p-8"><h1 className="text-3xl font-black">Create account</h1><input className="input mt-6" placeholder="Full name" value={f.name} onChange={e=>setF({...f,name:e.target.value})}/><input className="input mt-4" placeholder="Email" type="email" value={f.email} onChange={e=>setF({...f,email:e.target.value})}/><select className="input mt-4" value={f.role} onChange={e=>setF({...f,role:e.target.value})}><option value="candidate">Candidate</option><option value="recruiter">Recruiter</option></select><input className="input mt-4" placeholder="Password" type="password" value={f.password} onChange={e=>setF({...f,password:e.target.value})}/><input className="input mt-4" placeholder="Confirm password" type="password" value={f.confirmPassword} onChange={e=>setF({...f,confirmPassword:e.target.value})}/><button className="btn-primary mt-6 w-full">Create Account</button><p className="mt-5 text-center text-sm">Already have account? <Link className="font-bold text-primary" to="/login">Login</Link></p></form></main>}
