import{useEffect,useState}from'react';import api from'../../services/api';import JobCard from'../../components/JobCard';export default function SavedJobs(){const[i,setI]=useState([]);useEffect(()=>{api.get('/candidate/saved-jobs').then(r=>setI(r.data))},[]);return <div><h1 className="text-3xl font-black">Saved Jobs</h1><div className="mt-6 grid gap-5 md:grid-cols-3">{i.map(s=><JobCard key={s._id} job={s.job}/>)}</div></div>}
