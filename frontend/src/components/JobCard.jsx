import{Link}from'react-router-dom';export default function JobCard({job,actions}){return <div className="card p-5 transition hover:-translate-y-1 hover:shadow-lg"><div className="flex justify-between gap-3"><div><Link to={`/jobs/${job._id}`} className="text-xl font-bold hover:text-primary">{job.title}</Link><p className="text-sm text-slate-500">{job.company?.companyName||'Company'} · {job.location}</p></div><span className="rounded-full bg-indigo-50 px-3 py-1 text-xs font-bold text-primary">{job.jobType}</span></div><p className="mt-4 line-clamp-2 text-sm text-slate-600">{job.description}</p><div className="mt-4 flex flex-wrap gap-2">{(job.skills||[]).slice(0,5).map(s=><span key={s} className="rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold">{s}</span>)}</div><div className="mt-5 flex gap-2"><Link to={`/jobs/${job._id}`} className="btn-primary py-2">View Details</Link>{actions}</div></div>}
